"use client";

export default function InteractiveFeatureDetail({ feature }) {
  return (
    <div className="mt-4 glass rounded-lg p-3 animate-reveal">
      <p className="text-sm text-purple-300">
        Click to explore →
      </p>
    </div>
  );
}